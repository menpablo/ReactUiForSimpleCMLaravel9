# ReactUiForSimpleCMLaravel9

## UI for SimpleCMLaravel9. It lists and creates Posts and allows comments

### Config
    Copy env.example to `.env` file and set up the new API connection url.

### Quickguide
    $ npm install
    $ npm run dev